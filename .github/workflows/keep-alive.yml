name: Keep Render app awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -sf -o /dev/null https://trivia-poc.onrender.com/ || echo "ping failed, app may be cold-starting"
